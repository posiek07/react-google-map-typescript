import { useState, useEffect } from "react";
import useFetchPoints from "./useFetchPoints";

// export default function useSelectedMarkerProduct() {
//     const [selectedMarkerProducts, setSelectedMarkerProducts] = useState<[se]>(
//         []
//     );

//   const setSelectedMarkerProductsHandler = (product: any) => {
//     setSelectedMarkerProducts(product)
//   }

//     return [selectedMarkerProducts, setSelectedMarkerProductsHandler]
// }
